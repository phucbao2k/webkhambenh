<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Get crud</title>
    <style>
table {
  font-family: arial, sans-serif;
  border-collapse: collapse;
  width: 100%;
}

td, th {
  border: 1px solid #dddddd;
  text-align: left;
  padding: 8px;
}
.cap{
    background-color: green;
}


</style>
</head>
<body>
    <h1>get crud from view</h1>
    <table style="width: 100%;">
        <caption class="cap">User data</caption>
        <tr>
            <th>Email</th>
            <th>Firs Name</th>
            <th>Last Name</th>
            <th>Address</th>
            <th>Action</th>
        </tr>
        <% for(let i=0; i<dataTable.length; i++) { %>
            <tr>
                <td><%= dataTable[i].email %></td>
                <td><%= dataTable[i].firstName %></td>
                <td><%= dataTable[i].lastName %></td>
                <td><%= dataTable[i].address %></td>
                <td>
                    <a href="/edit-crud?id=<%= dataTable[i].id %>" style="text-decoration: none; cursor: pointer;" >Edit</a>
                    <a href="/delete-crud?id=<%= dataTable[i].id %>" style="text-decoration: none; cursor: pointer;" >Delete</a>
                </td>
            </tr>
         <% } %>
    </table>
</body>
</html>